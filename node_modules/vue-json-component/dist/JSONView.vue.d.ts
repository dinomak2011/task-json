import Vue, { VueConstructor } from 'vue';
declare const _default: VueConstructor<{
    build: (key: string, val: any, depth: number, path: string, includeKey: boolean) => object;
    isObject: (val: any) => boolean;
    isArray: (val: any) => boolean;
    itemSelected: (data: object) => void;
} & {
    parsed: object;
    hasSelectedListener: boolean;
} & {
    data: {};
    rootKey: string;
    maxDepth: number;
    colorScheme: string;
} & Vue>;
export default _default;
